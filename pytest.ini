[pytest]
markers =
    integration: marks tests that require external dependencies or assets (deselect with '-m "not integration"')
    slow: marks tests as slow (deselect with '-m "not slow"')
addopts = -q

